<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark brand theme applied before Compose takes over (avoids a white launch flash). -->
    <style name="Theme.SeasonSprint" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/brand_bg</item>
        <item name="android:statusBarColor">@color/brand_bg</item>
        <item name="android:navigationBarColor">@color/brand_bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
